# integration tests

> These tests dependencies on output or connect to development servers

eg. run lathe/sifter before running these tests or bring up gen3 instance

specifically: many of these integration tests depend on a
development server (eg. https://aced-training.compbio.ohsu.edu/) with accompanying credentials file in ~/.gen3/credentials.json

* __run prior every push!__
